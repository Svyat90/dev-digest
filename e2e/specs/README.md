# specs — e2e

Browser flows (`NN-name.flow.json`) executed by `run.ts` — not written specs,
which live in `e2e/docs/`. The runner ignores anything that is not
`*.flow.json`. Flow format and coverage table: `e2e/README.md`.
